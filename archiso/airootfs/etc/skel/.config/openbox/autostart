#sleep 5; termite --geometry=650x450 --exec=/usr/bin/al-hello &
pactl set-sink-mute @DEFAULT_SINK@ false &

$HOME/.config/openbox/scripts/compositor.sh &

nitrogen --restore &

# panel
$HOME/.config/polybar/scripts/launch.sh &

# Openbox autostart
ob-autostart -d &

# Required for xfce setting to work
xfsettingsd &
