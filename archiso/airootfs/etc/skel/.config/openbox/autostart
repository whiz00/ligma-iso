#sleep 3; termite --geometry=650x450 --exec=/usr/bin/al-hello &
pactl set-sink-mute @DEFAULT_SINK@ false &

compton -b --config $HOME/.config/compton.conf &

nitrogen --restore &

# panel
$HOME/.config/polybar/scripts/launch.sh &

# Openbox autostart
ob-autostart -d &

# Required for xfce setting to work
xfsettingsd &
